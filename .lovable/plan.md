
The user wants:
1. Delete the green/yellow/red vertical bar (visible in screenshot — likely `EqualizerBars` or a similar FX element on the left edge).
2. The tempo fader currently sits on the right edge — they say it's "on top of the page" obscuring content. Move it to where the deleted bar was (left side, out of the way).

Let me check what that colorful bar component is.
